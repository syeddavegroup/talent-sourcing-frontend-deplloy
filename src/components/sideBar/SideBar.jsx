import { Box } from '@mui/material';
import React from 'react';

function SideBar({ children }) {
  return (
    <Box>
      <Box sx={{ display: 'flex' }}>
        <Box> cdsvsdvsvf</Box>
        <Box>{children}</Box>
      </Box>
    </Box>
  );
}

export default SideBar;
